fastapi==0.115.0
uvicorn[standard]==0.30.6
scikit-learn==1.5.2
lightgbm==4.5.0
joblib==1.4.2
requests==2.32.3
python-dotenv==1.0.1
pandas==2.2.2
pytest==8.3.2
httpx==0.27.2
cachetools==5.5.2
# MLOps dependencies
numpy>=1.26.0           # explicit (was transitive via pandas/lightgbm)
scipy>=1.13.0           # KS test in training/drift.py
pyarrow>=16.0.0         # parquet read/write for retraining dataset
# Azure ML SDK — optional; only needed when AZURE_* env vars are configured.
# Install manually if using Azure ML registration:
#   pip install azure-ai-ml azure-identity
# azure-ai-ml>=1.15.0
# azure-identity>=1.17.0
